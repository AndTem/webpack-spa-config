const DEFAULT_PUBLIC_PATH = '/';
const DEFAULT_SCRIPTS_FILE_NAME = '[hash].bundle.js';

const DEFAULT_RESOLVE_EXTENSIONS = [
  '.js',
  '.jsx',
  '.json',
  '.css',
  '.scss',
  '.less',
];

const DEFAULT_RESOLVE_MODULES = ['node_modules'];

export {
  DEFAULT_PUBLIC_PATH,
  DEFAULT_SCRIPTS_FILE_NAME,
  DEFAULT_RESOLVE_EXTENSIONS,
  DEFAULT_RESOLVE_MODULES,
};
